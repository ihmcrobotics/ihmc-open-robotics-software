plugins {
    id("us.ihmc.ihmc-build") version "0.18.0"
    id("us.ihmc.ihmc-ci") version "4.25"
   id("us.ihmc.ihmc-cd") version "0.1"
    id("us.ihmc.log-tools") version "0.3.1"
}

ihmc {
    loadProductProperties("../product.properties")

    configureDependencyResolution()
    configurePublications()
}

mainDependencies {
    compile("us.ihmc:ihmc-quadruped-planning:source")
    compile("us.ihmc:ihmc-path-planning-data-sets:source")
    compile("us.ihmc:ihmc-common-walking-control-modules:source")

}

testDependencies {
    compile ihmc.sourceSetProject("visualizers")
    compile("us.ihmc:ihmc-quadruped-planning-test:source")
}

visualizersDependencies {
    compile ihmc.sourceSetProject("main")

    compile("us.ihmc:ihmc-robot-models-visualizers:source")
    compile("us.ihmc:ihmc-path-planning-visualizers:source")
    compile("us.ihmc:ihmc-common-walking-control-modules:source")
}