apply plugin: 'java'
apply plugin: 'jacoco'
apply from: 'gradleScripts/util.gradle'
evaluationDependsOn(':IHMCOpenRoboticsSoftware')

sourceCompatibility = 1.7
version = "${project(':IHMCOpenRoboticsSoftware').version}"
project.ext.fullVersion = "${project(':IHMCOpenRoboticsSoftware').ext.fullVersion}"
project.ext.licenseURL = "http://www.apache.org/licenses/LICENSE-2.0.txt"
project.ext.licenseName = "Apache License, Version 2.0"
project.ext.bintrayLicenseName = "Apache-2.0"

setupDefaultDirectoryStructure()

setupDefaultRepositories()

setupProjectSpecificDependencies()

jar {
    manifest {
        attributes(
                "Created-By": "IHMC Gradle Build Script",
                "Implementation-Title": project.name,
                "Implementation-Version": project.version,
                "Implementation-Vendor": "IHMC",

                "Bundle-Name": project.name,
                "Bundle-Version": project.version,
                "Bundle-License": "${project.ext.licenseURL}",
                "Bundle-Vendor": "IHMC")
    }
}

test.finalizedBy jacocoTestReport
