rootProject.name = 'ihmc-robotics-toolkit'

println 'Evaluating IHMCRoboticsToolkit settings.gradle'

def includes = [
'../IHMCJavaExtensions',
]

if (startParameter.searchUpwards)
   includes.each { include ->
      if (new File(rootProject.projectDir, include).exists())
         includeBuild include
   }
