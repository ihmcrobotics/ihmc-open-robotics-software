#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../crowdin-cli/bin/crowdin-cli" "$@"
  ret=$?
else 
  node  "$basedir/../crowdin-cli/bin/crowdin-cli" "$@"
  ret=$?
fi
exit $ret
