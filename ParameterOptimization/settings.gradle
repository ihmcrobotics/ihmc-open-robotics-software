rootProject.name = 'parameter-optimization'

println 'Evaluating ParameterOptimization settings.gradle'

def includes = [
'../IHMCRoboticsToolkit',
'../IHMCJavaExtensions',
]

if (startParameter.searchUpwards)
   includes.each { include ->
      if (new File(rootProject.projectDir, include).exists())
         includeBuild include
   }
