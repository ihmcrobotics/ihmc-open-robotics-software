{
    "description": "Parameter extractor for SCAN: identifies all objects the robot must find during scene scanning.",

    "api_base": "http://localhost:8000/v1",
    "model": "auto",
    "temperature": 0.0,
    "max_tokens": 256,
    "top_p": 1.0,
    "supports_vision": true,
    "image": "../test/Barrier.png",
    "prompt_type": "scan_param_extractor",

    "system_prompt": [
        "You are an intelligent assistant for a robot operating in a dynamic, partially observable environment.",
        "Your task is to identify every object the robot will need to interact with, based on the task description.",
        "",
        "Naming convention: each object is identified by its base name (e.g., person, charge).",
        "Include all objects the robot must navigate to, receive from, place on, or otherwise act upon."
    ],

    "task_description": [
        "1. SCAN the environment to populate scene_objects.",
        "2. GOTO (to the person to the right of the barrier).",
        "3. RECEIVE CHARGE.",
        "4. GOTO (to the front of the DoorPanel).",
        "5. PLACE CHARGE ON DOOR.",
        "6. GOTO (behind the barrier)."
    ],

    "examples": [
        "Example 1:",
        "  task_description:",
        "    1. SCAN the environment.",
        "    2. GOTO (to the person behind the couch).",
        "    3. RECEIVE CHARGE.",
        "    4. GOTO (to the front of the door_panel).",
        "    5. PLACE CHARGE ON DOOR.",
        "    6. GOTO (behind the couch).",
        "  Output: { \"target_objects\": [\"person\", \"couch\", \"charge\", \"door_panel\"] }",
        "",
        "Example 2:",
        "  task_description:",
        "    1. SCAN the environment.",
        "    2. GOTO (to the person in front of the wall).",
        "    3. RECEIVE CHARGE.",
        "  Output: { \"target_objects\": [\"person\", \"wall\", \"charge\"] }"
    ],

    "immediate_task": "List all objects the robot must interact with to complete the task.",
    "precognition": "Think through each step: what does the robot navigate to, receive, or act on?",
    "output_format": [
        "Return a valid JSON object:",
        "  { target_objects: [objectName, objectName, ...] }",
        "",
        "Use base name with no numbers(e.g., person). No explanations or code."
    ]
}
