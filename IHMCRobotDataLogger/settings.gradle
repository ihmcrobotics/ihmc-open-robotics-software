rootProject.name = 'ihmc-robot-data-logger'

println 'Evaluating IHMCRobotDataLogger settings.gradle'

def includes = [
'../ModelFileLoader',
'../IHMCJavaExtensions',
'../IHMCRoboticsToolkit',
'../IHMCGraphicsDescription',
'../IHMCRobotDescription',
]

if (startParameter.searchUpwards)
   includes.each { include ->
      if (new File(rootProject.projectDir, include).exists())
         includeBuild include
   }
