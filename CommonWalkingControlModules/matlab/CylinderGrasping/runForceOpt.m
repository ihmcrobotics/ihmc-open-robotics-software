
function [rho]=runForceOpt(C,Q,c,rhoMin, rhoMax,wRho)
cvx_begin
  % Caution: automatically generated by cvxgen. May be incorrect.
  variable rho(50, 1);
  
  minimize(quad_form(Q*rho - c, C) + wRho*quad_form(rho, eye(50)));
  subject to
    rho >= rhoMin;
    rhoMax >= rho;
cvx_end
end