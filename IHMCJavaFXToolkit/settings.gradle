rootProject.name = 'ihmc-javafx-toolkit'

println 'Evaluating IHMCJavaFXToolkit settings.gradle'

def includes = [
'../IHMCGraphicsDescription',
'../IHMCJavaToolkit',
'../IHMCRoboticsToolkit',
]

if (startParameter.searchUpwards)
   includes.each { include ->
      if (new File(rootProject.projectDir, include).exists())
         includeBuild include
   }
