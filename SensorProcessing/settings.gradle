rootProject.name = 'sensor-processing'

println 'Evaluating SensorProcessing settings.gradle'

def includes = [
'../IHMCCommunication',
'../KalmanProject',
'../IHMCJavaToolkit',
'../IHMCJavaExtensions',
'../IHMCRoboticsToolkit',
'../IHMCGraphicsDescription',
'../IHMCRobotModels',
]

if (startParameter.searchUpwards)
   includes.each { include ->
      if (new File(rootProject.projectDir, include).exists())
         includeBuild include
   }
