rootProject.name = 'simulation-construction-set-tools'

println 'Evaluating SimulationConstructionSetTools settings.gradle'

def includes = [
'../SimulationConstructionSet',
]

if (startParameter.searchUpwards)
   includes.each { include ->
      if (new File(rootProject.projectDir, include).exists())
         includeBuild include
   }
