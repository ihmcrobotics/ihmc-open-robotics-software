IHMC Atlas Gazebo Sim Plugin

To build the plugin:

	Run 'cmake ..' in Atlas/build
	
	Make all
	
To run the plugin in Gazebo:

	Add these lines to your .bashrc (replace [your_workspace_here]):
	
		export ROS_PACKAGE_PATH=$ROS_PACKAGE_PATH:[your_workspace_here]/ROSCatkinWorkspace/catkin_ws
		export GAZEBO_PLUGIN_PATH=$GAZEBO_PLUGIN_PATH:[your_workspace_here]/ROSCatkinWorkspace/catkin_ws/ihmc_gazebo/lib
		
	(for now) Make a symlink from ROSCatkinWorkspace/catkin_ws to Atlas/ihmc_gazebo using:
	
		ln -s R ROSCatkinWorkspace/catkin_ws/ihmc_gazebo Atlas/ihmc_gazebo
	
	Make sure you can:
	
		roscd ihmc_gazebo
	
	Then run:
	
		roslaunch ihmc_gazebo ihmc_atlas.launch

	