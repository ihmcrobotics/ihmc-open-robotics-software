/*
 * This class was automatically generated by jros2.
 * Do not modify this file directly.
 */
package behavior_msgs;

import us.ihmc.fastddsjava.cdr.CDRBuffer;
import us.ihmc.fastddsjava.cdr.idl.*;
import us.ihmc.jros2.ROS2Message;

/**
A message for remotely operating the VLA policies
<p>Source (behavior_msgs/VLAOperationMessage):
<pre>{@code
# A message for remotely operating the VLA policies

ihmc_common_msgs/LatestModificationMessage latest_timestamp_modifiable

# Monotonically increasing, number of updates of robot thread
uint32 sequence_id

# Whether the inference and action output to IK streaming is active
bool running

# Whether to enable controlling the robot instead of just previewing
bool control_robot

# The latest action hand poses
geometry_msgs/Pose[2] action_hand_poses

# The latest action forearm poses
geometry_msgs/Pose[2] action_forearm_poses

# Status message from robot thread to show to the operator
string status_message
}</pre>
*/
public class VLAOperationMessage implements ROS2Message<VLAOperationMessage>
{
   public static final java.lang.String name = "behavior_msgs::msg::dds_::VLAOperationMessage_";

   private final ihmc_common_msgs.LatestModificationMessage latest_timestamp_modifiable_;
   /**
      Monotonically increasing, number of updates of robot thread
   */
   private int sequence_id_;
   /**
      Whether the inference and action output to IK streaming is active
   */
   private boolean running_;
   /**
      Whether to enable controlling the robot instead of just previewing
   */
   private boolean control_robot_;
   /**
      The latest action hand poses
   */
   private final geometry_msgs.Pose[] action_hand_poses_;
   /**
      The latest action forearm poses
   */
   private final geometry_msgs.Pose[] action_forearm_poses_;
   /**
      Status message from robot thread to show to the operator
   */
   private final StringBuilder status_message_;

   public VLAOperationMessage()
   {
      latest_timestamp_modifiable_ = new ihmc_common_msgs.LatestModificationMessage();
      running_ = (boolean) false;
      control_robot_ = (boolean) false;
      action_hand_poses_ = new geometry_msgs.Pose[2];
      // action_hand_poses is defined as a fixed-size array, so it is pre-allocated.
      for (int i = 0; i < action_hand_poses_.length; ++i)
      {
         action_hand_poses_[i] = new geometry_msgs.Pose();
      }
      action_forearm_poses_ = new geometry_msgs.Pose[2];
      // action_forearm_poses is defined as a fixed-size array, so it is pre-allocated.
      for (int i = 0; i < action_forearm_poses_.length; ++i)
      {
         action_forearm_poses_[i] = new geometry_msgs.Pose();
      }
      status_message_ = new StringBuilder();

   }

   @Override
   public int calculateSizeBytes(int currentAlignment)
   {
      int initialAlignment = currentAlignment;

      currentAlignment += latest_timestamp_modifiable_.calculateSizeBytes(currentAlignment);
      currentAlignment += 4 + CDRBuffer.alignment(currentAlignment, 4); // sequence_id_
      currentAlignment += 1 + CDRBuffer.alignment(currentAlignment, 1); // running_
      currentAlignment += 1 + CDRBuffer.alignment(currentAlignment, 1); // control_robot_
      for (int i = 0; i < action_hand_poses_.length; ++i)
      {
         currentAlignment += action_hand_poses_[i].calculateSizeBytes(currentAlignment);
      }
      for (int i = 0; i < action_forearm_poses_.length; ++i)
      {
         currentAlignment += action_forearm_poses_[i].calculateSizeBytes(currentAlignment);
      }
      currentAlignment += 4 + CDRBuffer.alignment(currentAlignment, 4) + (1 * status_message_.length()) + 1; // status_message_

      return currentAlignment - initialAlignment;
   }

   @Override
   public void serialize(CDRBuffer buffer)
   {
      latest_timestamp_modifiable_.serialize(buffer);
      buffer.writeInt(sequence_id_);
      buffer.writeBoolean(running_);
      buffer.writeBoolean(control_robot_);
      for (int i = 0; i < action_hand_poses_.length; ++i)
      {
         action_hand_poses_[i].serialize(buffer);
      }
      for (int i = 0; i < action_forearm_poses_.length; ++i)
      {
         action_forearm_poses_[i].serialize(buffer);
      }
      buffer.writeString(status_message_);

   }

   @Override
   public void deserialize(CDRBuffer buffer)
   {
      latest_timestamp_modifiable_.deserialize(buffer);
      sequence_id_ = buffer.readInt();
      running_ = buffer.readBoolean();
      control_robot_ = buffer.readBoolean();
      for (int i = 0; i < action_hand_poses_.length; ++i)
      {
         action_hand_poses_[i].deserialize(buffer);
      }
      for (int i = 0; i < action_forearm_poses_.length; ++i)
      {
         action_forearm_poses_[i].deserialize(buffer);
      }
      buffer.readString(status_message_);

   }

   @Override
   public void set(VLAOperationMessage from)
   {
      latest_timestamp_modifiable_.set(from.latest_timestamp_modifiable_);
      sequence_id_ = from.sequence_id_;
      running_ = from.running_;
      control_robot_ = from.control_robot_;
      for (int i = 0; i < action_hand_poses_.length; ++i)
      {
         action_hand_poses_[i].set(from.action_hand_poses_[i]);
      }
      for (int i = 0; i < action_forearm_poses_.length; ++i)
      {
         action_forearm_poses_[i].set(from.action_forearm_poses_[i]);
      }
      status_message_.delete(0, status_message_.length());
      status_message_.insert(0, from.status_message_);

   }

   public ihmc_common_msgs.LatestModificationMessage getLatestTimestampModifiable()
   {
      return latest_timestamp_modifiable_;
   }

   public int getSequenceId()
   {
      return sequence_id_;
   }

   public void setSequenceId(int sequence_id_)
   {
      this.sequence_id_ = sequence_id_;
   }

   public boolean getRunning()
   {
      return running_;
   }

   public void setRunning(boolean running_)
   {
      this.running_ = running_;
   }

   public boolean getControlRobot()
   {
      return control_robot_;
   }

   public void setControlRobot(boolean control_robot_)
   {
      this.control_robot_ = control_robot_;
   }

   public geometry_msgs.Pose[] getActionHandPoses()
   {
      return action_hand_poses_;
   }

   public geometry_msgs.Pose[] getActionForearmPoses()
   {
      return action_forearm_poses_;
   }

   public StringBuilder getStatusMessage()
   {
      return status_message_;
   }

   public java.lang.String getStatusMessageAsString()
   {
      return status_message_.toString();
   }

   public void setStatusMessage(java.lang.String s)
   {
      this.status_message_.delete(0, this.status_message_.length());
      this.status_message_.insert(0, s);
   }

   @Override
   public java.lang.String toString()
   {
      java.lang.StringBuilder builder = new java.lang.StringBuilder();
      builder.append("VLAOperationMessage {");
      builder.append("latest_timestamp_modifiable_=");
      builder.append(latest_timestamp_modifiable_);
      builder.append("sequence_id_=");
      builder.append(sequence_id_);
      builder.append("running_=");
      builder.append(running_);
      builder.append("control_robot_=");
      builder.append(control_robot_);
      builder.append("action_hand_poses_=");
      builder.append(java.util.Arrays.toString(action_hand_poses_));
      builder.append("action_forearm_poses_=");
      builder.append(java.util.Arrays.toString(action_forearm_poses_));
      builder.append("status_message_=");
      builder.append(status_message_.toString());

      builder.append("}");
      return builder.toString();
   }
}