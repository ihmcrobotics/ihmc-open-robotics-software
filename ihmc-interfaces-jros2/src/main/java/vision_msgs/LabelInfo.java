/*
 * This class was automatically generated by jros2.
 * Do not modify this file directly.
 */
package vision_msgs;

import us.ihmc.fastddsjava.cdr.CDRBuffer;
import us.ihmc.fastddsjava.cdr.idl.*;
import us.ihmc.jros2.ROS2Message;

/**
Provides meta-information about a visual pipeline.

This message serves a similar purpose to sensor_msgs/CameraInfo, but instead
of being tied to hardware, it represents information about a specific
computer vision pipeline. This information stays constant (or relatively
constant) over time, and so it is wasteful to send it with each individual
result. By listening to these messages, subscribers will receive
the context in which published vision messages are to be interpreted.
Each vision pipeline should publish its LabelInfo messages to its own topic,
in a manner similar to CameraInfo.
This message is meant to allow converting data from vision pipelines that
return id based classifications back to human readable string class names.
<p>Source (vision_msgs/LabelInfo):
<pre>{@code
# Provides meta-information about a visual pipeline.
#
# This message serves a similar purpose to sensor_msgs/CameraInfo, but instead
#   of being tied to hardware, it represents information about a specific
#   computer vision pipeline. This information stays constant (or relatively
#   constant) over time, and so it is wasteful to send it with each individual
#   result. By listening to these messages, subscribers will receive
#   the context in which published vision messages are to be interpreted.
# Each vision pipeline should publish its LabelInfo messages to its own topic,
#   in a manner similar to CameraInfo.
# This message is meant to allow converting data from vision pipelines that
#   return id based classifications back to human readable string class names.

# Used for sequencing
std_msgs/Header header

# An array of uint16 keys and string values containing the association
#   between class identifiers and their names. According to the amount
#   of classes and the datatype used to store their ids internally, the
#   maxiumum class id allowed (65535 for uint16 and 255 for uint8) belongs to
#   the "UNLABELED" class.
vision_msgs/VisionClass[] class_map 

# The value between 0-1 used as confidence threshold for the inference.
float32 threshold
}</pre>
*/
public class LabelInfo implements ROS2Message<LabelInfo>
{
   public static final java.lang.String name = "vision_msgs::msg::dds_::LabelInfo_";

   /**
      Used for sequencing
   */
   private final std_msgs.Header header_;
   /**
      An array of uint16 keys and string values containing the association
      between class identifiers and their names. According to the amount
      of classes and the datatype used to store their ids internally, the
      maxiumum class id allowed (65535 for uint16 and 255 for uint8) belongs to
      the "UNLABELED" class.
   */
   private final IDLObjectSequence<vision_msgs.VisionClass> class_map_;
   /**
      The value between 0-1 used as confidence threshold for the inference.
   */
   private float threshold_;

   public LabelInfo()
   {
      header_ = new std_msgs.Header();
      class_map_ = new IDLObjectSequence<vision_msgs.VisionClass>(vision_msgs.VisionClass.class);

   }

   public LabelInfo(LabelInfo from)
   {
      this();
      set(from);
   }

   @Override
   public int calculateSizeBytes(int currentAlignment)
   {
      int initialAlignment = currentAlignment;

      currentAlignment += header_.calculateSizeBytes(currentAlignment);
      currentAlignment += class_map_.calculateSizeBytes(currentAlignment);
      currentAlignment += 4 + CDRBuffer.alignment(currentAlignment, 4); // threshold_

      return currentAlignment - initialAlignment;
   }

   @Override
   public void serialize(CDRBuffer buffer)
   {
      header_.serialize(buffer);
      class_map_.serialize(buffer);
      buffer.writeFloat(threshold_);

   }

   @Override
   public void deserialize(CDRBuffer buffer)
   {
      header_.deserialize(buffer);
      class_map_.deserialize(buffer);
      threshold_ = buffer.readFloat();

   }

   @Override
   public void set(LabelInfo from)
   {
      header_.set(from.header_);
      class_map_.set(from.class_map_);
      threshold_ = from.threshold_;

   }

   public std_msgs.Header getHeader()
   {
      return header_;
   }

   public IDLObjectSequence<vision_msgs.VisionClass> getClassMap()
   {
      return class_map_;
   }

   public float getThreshold()
   {
      return threshold_;
   }

   public void setThreshold(float threshold_)
   {
      this.threshold_ = threshold_;
   }


   @Override
   public java.lang.String toString()
   {
      java.lang.StringBuilder builder = new java.lang.StringBuilder();
      builder.append("LabelInfo {");
      builder.append("header_=");
      builder.append(header_);
      builder.append("class_map_=");
      builder.append(class_map_);
      builder.append("threshold_=");
      builder.append(threshold_);

      builder.append("}");
      return builder.toString();
   }
}