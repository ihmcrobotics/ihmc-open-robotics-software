String baseDir = getProjectDir()

new File(baseDir + File.separator + 'src/main/java').mkdir()
new File(baseDir + File.separator + 'src/test/java').mkdir()
new File(baseDir + File.separator + 'test').mkdir()
new File(baseDir + File.separator + 'gradleScripts').mkdir()
new File(baseDir + File.separator + 'gradleScripts' + File.separator + 'dependencies.gradle').createNewFile()

sourceSets
{
    main
    {
        java
        {
            srcDirs = ['src/main/java', 'test']
        }

        resources
        {
        }
    }

    test
    {
        java
        {
            srcDirs = ['src/test/java']
        }
    }
}
