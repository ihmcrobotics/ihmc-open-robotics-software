rootProject.name = 'ihmc-system-identification'

println 'Evaluating IHMCSystemIdentification settings.gradle'

def includes = [
'../IHMCRoboticsToolkit',
'../IHMCGraphicsDescription',
'../SimulationConstructionSet',
'../IHMCJavaExtensions',
]

if (startParameter.searchUpwards)
   includes.each { include ->
      if (new File(rootProject.projectDir, include).exists())
         includeBuild include
   }
